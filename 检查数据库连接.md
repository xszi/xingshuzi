# 检查Server与MySQL数据库连接

## 🔍 在ECS上执行以下检查命令

### 方法一：检查API容器日志

```bash
# 查看后端容器日志（查找数据库连接信息）
docker logs xingshuzi-api | tail -50

# 实时查看日志
docker logs -f xingshuzi-api
```

**正常情况应该看到：**
- ✅ 应用启动成功
- ✅ 没有数据库连接错误
- ✅ 可能看到类似 "Running on http://..." 的信息

**如果有问题会看到：**
- ❌ `Can't connect to MySQL server`
- ❌ `Access denied for user`
- ❌ `Unknown database`

---

### 方法二：进入容器测试数据库连接

```bash
# 进入API容器
docker exec -it xingshuzi-api sh

# 在容器内执行Python测试
python3 << 'EOF'
import pymysql
import os

try:
    conn = pymysql.connect(
        host=os.getenv('MYSQL_HOST', '120.76.247.123'),
        port=int(os.getenv('MYSQL_PORT', '3306')),
        user=os.getenv('MYSQL_USER', 'root'),
        password=os.getenv('MYSQL_PASSWORD', 'MyStrongRootPassword123'),
        database=os.getenv('MYSQL_DB', 'xingshuzi')
    )
    print("✅ 数据库连接成功！")
    
    cursor = conn.cursor()
    cursor.execute("SELECT DATABASE()")
    db = cursor.fetchone()
    print(f"当前数据库: {db[0]}")
    
    cursor.execute("SHOW TABLES")
    tables = cursor.fetchall()
    print(f"数据表数量: {len(tables)}")
    print("数据表列表:")
    for table in tables:
        print(f"  - {table[0]}")
    
    conn.close()
except Exception as e:
    print(f"❌ 数据库连接失败: {e}")
EOF

# 退出容器
exit
```

---

### 方法三：测试API接口

```bash
# 测试健康检查接口
curl -i http://localhost:5001/api/home/banners

# 如果返回 200 和 JSON数据，说明：
# ✅ API服务正常
# ✅ 数据库连接正常
```

**预期返回：**
```json
HTTP/1.1 200 OK
Content-Type: application/json

{"code":200,"data":[],"msg":"success"}
```

---

### 方法四：检查环境变量配置

```bash
# 查看API容器的环境变量
docker exec xingshuzi-api env | grep MYSQL

# 应该看到类似：
# MYSQL_HOST=120.76.247.123
# MYSQL_PORT=3306
# MYSQL_DB=xingshuzi
# MYSQL_USER=root
# MYSQL_PASSWORD=MyStrongRootPassword123
```

---

### 方法五：从容器内ping数据库服务器

```bash
# 测试网络连通性
docker exec xingshuzi-api ping -c 3 120.76.247.123

# 测试MySQL端口
docker exec xingshuzi-api nc -zv 120.76.247.123 3306
# 或
docker exec xingshuzi-api telnet 120.76.247.123 3306
```

---

### 方法六：检查数据库是否有数据

```bash
# 进入容器
docker exec -it xingshuzi-api sh

# 运行Python脚本检查
python3 << 'EOF'
from app import create_app
from app.models.user import User

app = create_app()
with app.app_context():
    try:
        users = User.query.all()
        print(f"✅ 数据库连接成功！")
        print(f"用户数量: {len(users)}")
        for user in users:
            print(f"  - {user.username} ({user.role})")
    except Exception as e:
        print(f"❌ 查询失败: {e}")
EOF

exit
```

---

## 🔧 常见问题排查

### 问题1：Can't connect to MySQL server

**可能原因：**
- MySQL服务未启动
- 防火墙/安全组未开放3306端口
- MYSQL_HOST配置错误

**解决方法：**
```bash
# 检查MySQL容器状态
docker ps | grep mysql

# 如果使用独立MySQL容器
cd /root/projects/xingshuzi/mysql-standalone
docker compose ps

# 检查ECS安全组是否开放3306端口
# 在阿里云控制台检查
```

### 问题2：Access denied for user

**可能原因：**
- 用户名或密码错误
- 用户权限不足

**解决方法：**
```bash
# 检查.env文件
cat /root/projects/xingshuzi/server/.env | grep MYSQL

# 测试MySQL登录
mysql -h 120.76.247.123 -P 3306 -uroot -pMyStrongRootPassword123 -e "SELECT 1"
```

### 问题3：Unknown database 'xingshuzi'

**可能原因：**
- 数据库不存在

**解决方法：**
```bash
# 连接MySQL创建数据库
mysql -h 120.76.247.123 -P 3306 -uroot -pMyStrongRootPassword123 << 'EOF'
CREATE DATABASE IF NOT EXISTS xingshuzi DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES;
EOF

# 或进入MySQL容器
docker exec -it xingshuzi-mysql mysql -uroot -pMyStrongRootPassword123
CREATE DATABASE IF NOT EXISTS xingshuzi DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE xingshuzi;
SHOW TABLES;
EXIT;
```

---

## 📊 完整的健康检查脚本

将以下脚本保存为 `check_db.sh` 并执行：

```bash
#!/bin/bash

echo "=========================================="
echo "  检查后端与数据库连接"
echo "=========================================="

# 1. 检查容器状态
echo -e "\n1️⃣  检查容器状态："
docker ps | grep xingshuzi-api
if [ $? -eq 0 ]; then
    echo "✅ API容器运行中"
else
    echo "❌ API容器未运行"
    exit 1
fi

# 2. 检查环境变量
echo -e "\n2️⃣  检查环境变量："
docker exec xingshuzi-api env | grep MYSQL

# 3. 测试数据库连接
echo -e "\n3️⃣  测试数据库连接："
docker exec xingshuzi-api python3 -c "
import pymysql
import os
try:
    conn = pymysql.connect(
        host=os.getenv('MYSQL_HOST'),
        port=int(os.getenv('MYSQL_PORT', '3306')),
        user=os.getenv('MYSQL_USER'),
        password=os.getenv('MYSQL_PASSWORD'),
        database=os.getenv('MYSQL_DB')
    )
    print('✅ 数据库连接成功')
    cursor = conn.cursor()
    cursor.execute('SHOW TABLES')
    tables = cursor.fetchall()
    print(f'数据表数量: {len(tables)}')
    conn.close()
except Exception as e:
    print(f'❌ 数据库连接失败: {e}')
    exit(1)
"

# 4. 测试API接口
echo -e "\n4️⃣  测试API接口："
RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:5001/api/home/banners)
if [ "$RESPONSE" == "200" ]; then
    echo "✅ API接口正常 (HTTP $RESPONSE)"
    curl -s http://localhost:5001/api/home/banners | python3 -m json.tool
else
    echo "❌ API接口异常 (HTTP $RESPONSE)"
fi

# 5. 查看最近日志
echo -e "\n5️⃣  最近日志（最后20行）："
docker logs xingshuzi-api --tail 20

echo -e "\n=========================================="
echo "  检查完成"
echo "=========================================="
```

**执行脚本：**
```bash
cd /root/projects/xingshuzi
chmod +x check_db.sh
./check_db.sh
```

---

## ✅ 正常连接的标志

如果数据库连接正常，您应该看到：

1. ✅ API容器运行中
2. ✅ 环境变量配置正确
3. ✅ Python数据库连接测试通过
4. ✅ API接口返回200状态码
5. ✅ 日志中没有数据库错误

---

## 🔄 如果连接失败，需要重新配置

```bash
# 1. 停止API容器
docker compose -f /root/projects/xingshuzi/server/docker-compose.yml down

# 2. 修改.env文件
nano /root/projects/xingshuzi/server/.env

# 确保以下配置正确：
MYSQL_HOST=120.76.247.123
MYSQL_PORT=3306
MYSQL_DB=xingshuzi
MYSQL_USER=root
MYSQL_PASSWORD=MyStrongRootPassword123

# 3. 重新启动
cd /root/projects/xingshuzi/server
docker compose up -d

# 4. 初始化数据库（如果需要）
docker exec xingshuzi-api python3 create_db.py
docker exec xingshuzi-api python3 init_admin.py

# 5. 重新检查
./check_db.sh
```

---

**立即开始检查：使用方法三测试API接口最简单！** 🚀

