version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xingshuzi-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
      - NUXT_PUBLIC_SITE_URL=http://120.76.247.123:3000
      - NUXT_PUBLIC_API_BASE=http://120.76.247.123:5001/api

