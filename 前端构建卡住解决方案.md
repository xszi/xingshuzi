# 前端构建卡住解决方案

## 🚨 立即执行（在ECS上）

### 第一步：停止当前构建

```bash
# 按 Ctrl+C 停止当前构建

# 或新开SSH连接执行
cd /root/projects/xingshuzi/web
docker compose down
docker stop $(docker ps -aq) 2>/dev/null || true
```

### 第二步：拉取最新代码

```bash
cd /root/projects/xingshuzi
git pull origin main
```

### 第三步：选择解决方案

---

## 📦 方案一：简化构建脚本（推荐）

适合：内存有限的服务器

```bash
cd /root/projects/xingshuzi/web
chmod +x deploy-simple.sh
./deploy-simple.sh
```

这个脚本会：
- ✅ 自动检查系统资源
- ✅ 设置合适的内存限制
- ✅ 显示构建进度
- ✅ 设置更长的超时时间

---

## 🔧 方案二：交互式修复脚本

提供3种构建方式选择：

```bash
cd /root/projects/xingshuzi/web
chmod +x fix-build.sh
./fix-build.sh
```

**选项说明：**

**选项1: 使用优化版Dockerfile构建**
- 适合：有2GB+内存的服务器
- 时间：5-8分钟
- 特点：完整的生产构建

**选项2: 在服务器上直接构建**
- 适合：Docker构建失败时
- 时间：3-5分钟
- 特点：跳过Docker多阶段构建

**选项3: 开发模式运行**
- 适合：快速测试
- 时间：1-2分钟
- ⚠️ 不推荐生产环境

---

## 🎯 方案三：手动逐步构建

如果自动脚本还是失败，手动执行：

```bash
cd /root/projects/xingshuzi/web

# 1. 停止并清理
docker compose down
docker system prune -f

# 2. 检查资源
free -h
df -h

# 3. 在服务器上直接构建（跳过Docker）
npm config set registry https://registry.npmmirror.com
npm ci
NODE_OPTIONS="--max-old-space-size=2048" npm run build

# 4. 使用已构建的文件创建简单镜像
cat > Dockerfile.simple << 'EOF'
FROM node:20-alpine
WORKDIR /app
COPY .output /app/.output
EXPOSE 3000
ENV NODE_ENV=production
CMD ["node", ".output/server/index.mjs"]
EOF

# 5. 构建并运行
docker build -f Dockerfile.simple -t xingshuzi-web .
docker run -d -p 3000:3000 --name xingshuzi-web --restart unless-stopped xingshuzi-web

# 6. 验证
curl -I http://localhost:3000
```

---

## 💡 方案四：升级服务器配置（临时）

如果预算允许，可以临时升级ECS配置：

```bash
# 在阿里云控制台：
# 1. 停止ECS实例
# 2. 升级配置到 2核8GB 或 4核8GB
# 3. 启动实例
# 4. 重新构建
cd /root/projects/xingshuzi/web
./deploy.sh

# 构建完成后，可以降回原配置
```

---

## 🔍 诊断工具

### 检查为什么卡住

```bash
# 查看系统资源
htop
# 或
top

# 查看内存使用
free -h

# 查看磁盘空间
df -h

# 查看Docker进程
ps aux | grep docker

# 查看Docker事件
docker events

# 查看构建日志
docker compose logs --tail 100
```

### 检查OOM（内存溢出）

```bash
# 查看系统日志
dmesg | grep -i "killed process"
dmesg | grep -i "out of memory"

# 如果看到 "Out of memory" 说明内存不足
```

---

## ⚙️ 优化配置

### 临时增加Swap（如果内存不足）

```bash
# 创建2GB swap文件
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 验证
free -h

# 构建完成后可以关闭
sudo swapoff /swapfile
sudo rm /swapfile
```

### 调整Docker资源限制

```bash
# 编辑 docker-compose.yml，添加资源限制
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G
```

---

## 📊 预期时间对比

| 方案 | 构建时间 | 成功率 | 推荐指数 |
|------|---------|--------|---------|
| 简化构建脚本 | 5-8分钟 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 服务器直接构建 | 3-5分钟 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 开发模式 | 1-2分钟 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 原始部署脚本 | 5-10分钟 | ⭐⭐⭐ | ⭐⭐ |

---

## ✅ 推荐流程

**对于2核4GB的ECS（最常见）：**

```bash
# 1. 先尝试简化构建
cd /root/projects/xingshuzi/web
./deploy-simple.sh

# 2. 如果还是卡住，尝试直接构建
./fix-build.sh
# 选择 2（在服务器上直接构建）

# 3. 如果还不行，添加Swap后重试
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
./deploy-simple.sh
```

---

## 🎯 最快速的解决方案

如果只是想快速看到效果（测试用）：

```bash
cd /root/projects/xingshuzi/web

# 使用开发模式（不构建，直接运行）
docker run -d \
  -p 3000:3000 \
  -v $(pwd):/app \
  -w /app \
  --name xingshuzi-web \
  --restart unless-stopped \
  node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install && npm run dev -- --host 0.0.0.0"

# 查看日志
docker logs -f xingshuzi-web
```

⚠️ 注意：开发模式不适合生产环境，只用于快速测试。

---

## 📞 如果所有方案都失败

可能的原因：
1. ❌ ECS配置太低（建议至少2核4GB + 2GB Swap）
2. ❌ 磁盘空间不足（检查 `df -h`）
3. ❌ 网络问题（检查npm镜像源）
4. ❌ 项目依赖有问题

**最终解决方案：**
在本地Mac或其他高配置机器上构建，然后推送Docker镜像到阿里云镜像仓库，在ECS上直接拉取。

---

**立即开始：先执行方案一的简化构建脚本！** 🚀

