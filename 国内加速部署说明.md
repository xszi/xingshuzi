# 国内加速部署说明

## 🚨 问题原因

部署时卡在下载依赖包，是因为：
- Debian apt 使用国外源，下载速度慢
- Python pip 使用国外 PyPI 源
- Node.js npm 使用国外 npm 源

## ✅ 已优化内容

### 1. Server Dockerfile 优化
- ✅ 使用阿里云 Debian 镜像源
- ✅ 使用阿里云 PyPI 镜像源
- ✅ 大幅提升 apt-get 和 pip install 速度

### 2. Web Dockerfile 优化
- ✅ 使用淘宝 npm 镜像源（npmmirror.com）
- ✅ 大幅提升 npm install 速度

## 🔧 如何使用优化后的配置

### 如果当前正在构建（卡住了）：

#### 方法一：Ctrl+C 停止当前构建

```bash
# 1. 在终端按 Ctrl+C 停止
# 2. 清理未完成的容器和镜像
cd /root/xingshuzi/server
docker compose down
docker system prune -f

# 3. 重新构建（使用优化后的 Dockerfile）
./deploy.sh
```

#### 方法二：新开终端强制停止

```bash
# 新开一个 SSH 连接
ssh root@120.76.247.123

# 查看正在构建的容器
docker ps -a

# 停止并删除
cd /root/xingshuzi/server
docker compose down
docker rmi xingshuzi-server-api 2>/dev/null

# 清理构建缓存
docker builder prune -f

# 重新构建
./deploy.sh
```

### 首次部署流程（使用优化配置）：

```bash
# 1. SSH登录ECS
ssh root@120.76.247.123

# 2. 克隆或更新代码
cd /root
git clone git@github.com:your-username/xingshuzi.git
# 或更新: cd /root/xingshuzi && git pull origin main

# 3. 部署后端（约2-3分钟）
cd /root/xingshuzi/server
cp .env.example .env
./deploy.sh

# 4. 部署前端（约3-5分钟）
cd /root/xingshuzi/web
./deploy.sh
```

## ⏱️ 预计构建时间

使用国内镜像源后：
- **Server后端**：2-3分钟
- **Web前端**：3-5分钟（首次构建较慢，后续更快）

之前使用国外源可能需要：
- Server：10-20分钟或更久
- Web：15-30分钟或更久

## 🔍 构建过程监控

### 查看实时日志：

```bash
# 后端构建日志
cd /root/xingshuzi/server
docker compose build

# 前端构建日志
cd /root/xingshuzi/web
docker compose build
```

### 检查构建进度：

```bash
# 查看Docker镜像
docker images | grep xingshuzi

# 查看容器状态
docker ps -a | grep xingshuzi
```

## 🛠️ 常用清理命令

### 清理失败的构建：

```bash
# 停止所有相关容器
docker compose down

# 删除相关镜像（重新构建）
docker rmi xingshuzi-server-api xingshuzi-web-web 2>/dev/null

# 清理构建缓存
docker builder prune -f

# 清理未使用的资源
docker system prune -f
```

### 完全重新开始：

```bash
# 警告：这会删除所有未使用的Docker资源
docker system prune -a -f

# 然后重新构建
cd /root/xingshuzi/server
./deploy.sh

cd /root/xingshuzi/web
./deploy.sh
```

## 📊 镜像源说明

### Debian APT 镜像源
- **使用**: 阿里云镜像 (mirrors.aliyun.com)
- **速度**: 国内访问快
- **可靠性**: 高

### Python PyPI 镜像源
- **使用**: 阿里云PyPI镜像 (mirrors.aliyun.com/pypi/simple/)
- **速度**: 快
- **备选**: 清华源 (pypi.tuna.tsinghua.edu.cn/simple)

### Node.js NPM 镜像源
- **使用**: 淘宝镜像 (registry.npmmirror.com)
- **速度**: 非常快
- **说明**: 这是淘宝npm镜像的新域名

## 🌐 如果在国外部署

如果您的ECS在国外（如新加坡、美国），可以还原为官方源：

### Server Dockerfile（国外版）：
```dockerfile
# 注释掉这行
# RUN sed -i 's/deb.debian.org/mirrors.aliyun.com/g' /etc/apt/sources.list.d/debian.sources

# 使用官方源
RUN pip install --no-cache-dir -r requirements.txt
```

### Web Dockerfile（国外版）：
```dockerfile
# 注释掉这行
# RUN npm config set registry https://registry.npmmirror.com

# 使用官方源
RUN npm ci
```

## ✅ 验证构建成功

### 后端验证：
```bash
# 检查容器运行
docker ps | grep xingshuzi-api

# 测试API
curl http://localhost:5001/api/home/banners

# 查看日志
docker logs xingshuzi-api
```

### 前端验证：
```bash
# 检查容器运行
docker ps | grep xingshuzi-web

# 测试Web
curl -I http://localhost:3000

# 查看日志
docker logs xingshuzi-web
```

## 🎯 快速命令参考

```bash
# === 停止当前构建 ===
Ctrl+C  # 或关闭终端

# === 清理并重新开始 ===
cd /root/xingshuzi/server
docker compose down
docker builder prune -f
./deploy.sh

cd /root/xingshuzi/web
docker compose down
docker builder prune -f
./deploy.sh

# === 查看状态 ===
docker ps                    # 运行中的容器
docker images               # 所有镜像
docker compose logs -f      # 实时日志

# === 完全清理 ===
docker system prune -a -f   # 清理所有未使用资源
```

## 💡 提示

1. **首次构建较慢**：即使使用国内镜像源，首次构建也需要下载基础镜像和依赖，请耐心等待

2. **后续构建更快**：Docker会缓存已下载的层，后续更新构建会快很多

3. **网络波动**：如果构建失败，通常是网络问题，重新运行 `./deploy.sh` 即可

4. **磁盘空间**：确保ECS有足够空间（建议至少10GB可用空间）

---

**使用国内镜像源，部署速度提升 5-10 倍！** 🚀

